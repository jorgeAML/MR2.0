doctype html
html 
    head    
        title MedicinaResources
        block scripts
            script(src="https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js")
            script(src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js")
            
            
    body
        block nav
            - var m = "{{ message }}"
            #app {{ message }}
            p here will be the nav
        block contentDefault
            h1 something here
        block footer
script(src="./vue/template01.js")

    